/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  // You can add other config options here if needed, for example:
  // images: { domains: ['your-image-domain.com'] },
};

export default nextConfig;